# 제안 목록

작업 중에 떠오른 개선 아이디어를 모아 둔다. **여기 적힌 것은 아직 구현하지 않은 것이다.**
바로 만들지 않고 기록해 두었다가, 필요할 때 골라서 진행한다.

## 상태 표시

| 상태 | 뜻 |
|---|---|
| 대기 | 아직 결정하지 않음 |
| 채택 | 하기로 정함 (아직 구현 전) |
| 완료 | 구현 끝남 — 완료된 항목은 맨 아래 「완료된 제안」으로 옮긴다 |
| 보류 | 지금은 안 함. 나중에 다시 봄 |
| 기각 | 하지 않기로 정함 (이유를 남긴다) |

---

*(진행 중인 제안 없음)*

---

## 완료된 제안

## P-001. 케이블 연속 입력 ([계속] 버튼)

- **제안일**: 2026-08-21
- **상태**: 완료 (2026-08-24)
- **관련 화면**: 케이블 추가 시트, 장비 추가 시트

**배경**
지금은 케이블을 한 건 추가하면 시트가 닫힌다. 다음 건을 적으려면 항목 카드에서 `+ 케이블`을 다시 눌러야 한다.
현장에서 한 항목에 케이블을 10건씩 적는 경우 시트를 열고 닫는 동작이 10번 반복된다.

**구현 내용**
추가 시트 하단을 `[취소] [추가] [계속]` 세 개로 늘렸다. `계속`을 누르면 저장한 뒤 시트를 닫지 않고 입력칸을 비우고
첫 칸으로 포커스를 되돌린다. 케이블·장비 시트 모두 같은 방식이다. 수정할 때는 `[취소] [저장]` 두 개 그대로다.

**원안에서 달라진 점**
- 버튼 이름을 `추가하고 계속` 대신 `계속` 으로 줄였다. 좁은 화면에서 세 버튼이 한 줄에 들어가야 한다.
- 직전 값을 남기지 않고 **전부 비운다**. 남은 값이 그대로 저장되는 실수를 막는 쪽을 택했다.
  장비의 구분(교체/신규)만은 시트를 열 때 고른 값으로 되돌린다.
- "n번째 추가됨" 표시는 토스트가 아니라 **시트 본문 맨 위 줄**에 넣었다. 시트가 브라우저 최상위 레이어에
  그려져 토스트가 뒤에 가리기 때문이다.

**남은 과제**
- `[취소]` 를 아이콘으로 줄이는 안은 쓰지 않았다. 글자 그대로 두고 버튼 좌우 여백만 줄여 한 줄에 담았다.
  더 좁은 기기에서 문제가 보이면 그때 다시 검토한다.
