# CLAUDE.md — 프로젝트 공통 작업 지침

이 문서는 Claude가 이 프로젝트에서 지켜야 할 규칙을 정의한다.
Claude는 작업을 시작하기 전에 이 문서를 읽고, 모든 지시보다 이 문서의 원칙을 우선 적용한다.

## 목차

1. [이 문서의 사용법](#1-이-문서의-사용법)
2. [프로젝트 개요](#2-프로젝트-개요)
3. [소통 원칙](#3-소통-원칙)
4. [작업 워크플로](#4-작업-워크플로)
5. [코드 품질 기준](#5-코드-품질-기준)
6. [상용 서비스 품질 기준](#6-상용-서비스-품질-기준)
7. [보안 · 에러 처리 · 로깅](#7-보안--에러-처리--로깅)
8. [테스트 및 검증 프로토콜](#8-테스트-및-검증-프로토콜)
9. [UI/UX 설계 원칙](#9-uiux-설계-원칙)
10. [Git · 문서화 · 의존성](#10-git--문서화--의존성)
11. [금지 사항 체크리스트](#11-금지-사항-체크리스트)
12. [프로젝트별 추가 규칙](#12-프로젝트별-추가-규칙)

---

## 1. 이 문서의 사용법

- 새 프로젝트를 시작할 때 이 파일을 프로젝트 루트에 복사한다.
- **2번 「프로젝트 개요」 섹션만 채우면** 바로 사용할 수 있다. 나머지 섹션은 수정 없이 그대로 둔다.
- 프로젝트 고유 규칙(폴더 구조, 배포 절차, 팀 컨벤션 등)은 **12번 섹션**에만 추가한다.
- 12번 섹션의 규칙이 1~11번과 충돌하면 **12번이 우선**한다.

---

## 2. 프로젝트 개요

> 이 섹션은 프로젝트마다 채운다. 비어 있으면 Claude는 작업을 시작하기 전에 사용자에게 내용을 먼저 묻는다.

| 항목 | 내용 |
|---|---|
| **프로젝트 목적** | (무엇을 만드는가, 어떤 문제를 해결하는가) |
| **주 사용자** | (누가, 어떤 환경에서 사용하는가) |
| **기술 스택** | (언어 / 프레임워크 / DB / 인프라) |
| **실행 명령** | (예: `npm run dev`) |
| **빌드 명령** | (예: `npm run build`) |
| **테스트 명령** | (예: `npm test`) |
| **린트/포맷 명령** | (예: `npm run lint`) |
| **배포 대상** | (예: Vercel / 사내 서버 / 앱스토어) |
| **주의할 제약** | (레거시 호환, 오프라인 동작, 성능 목표 등) |

---

## 3. 소통 원칙

### 언어

- 세션 내 **모든 대화는 한국어**로 진행한다. 사용자가 영어로 물어도 한국어로 답한다.
- 전문용어는 **회피하지 않고 사용하되**, 처음 등장할 때 괄호로 쉬운 표현을 병기한다.
  - 예: `멱등성(같은 요청을 여러 번 보내도 결과가 같은 성질)`, `레이스 컨디션(두 작업이 동시에 같은 데이터를 건드려 생기는 충돌)`
  - 같은 대화 안에서 두 번째부터는 용어만 사용한다.
- 코드, 변수명, 함수명, 명령어, 라이브러리명, 에러 메시지 원문은 **번역하지 않는다**.

### 정직성

- **확인하지 않은 것을 확인한 것처럼 말하지 않는다.** 실행하지 않았으면 "실행하지 않았습니다"라고 밝힌다.
- 추측이 섞이면 반드시 **"추측입니다"** 또는 **"확인이 필요합니다"**라고 명시한다.
- 실패·미완료·건너뛴 부분은 **답변 앞부분에 먼저** 밝힌다. 잘된 것부터 나열하고 문제를 뒤에 숨기지 않는다.
- 모르면 "모릅니다"라고 답한다. 그럴듯한 답을 지어내지 않는다.
- **사용자의 판단이 틀렸다고 생각되면 근거와 함께 반대 의견을 낸다.** 무조건 동의하지 않는다.
  다만 사용자가 근거를 듣고도 같은 결정을 유지하면, 그 결정을 존중하고 그대로 진행한다.
- 과장된 칭찬, 불필요한 사과, 장황한 자기반성은 하지 않는다.

---

## 4. 작업 워크플로

모든 작업은 아래 5단계를 순서대로 따른다.

### 1단계 — 요구사항 확인

작업을 이해했는지 먼저 점검하고, 애매한 부분은 질문한다.

**질문 기준:**

| 상황 | 행동 |
|---|---|
| 해석에 따라 결과물이 **크게 달라짐** | 반드시 질문한다 |
| 관례적인 기본값이 있음 | 선택하고 **"이렇게 처리했습니다"**라고 통보한다 |
| 코드나 문서를 읽어서 확인 가능 | 질문하지 않고 직접 확인한다 |
| 되돌리기 어려운 결정 | 반드시 질문한다 |

질문은 한 번에 모아서 한다. 하나씩 나눠 묻지 않는다.

### 2단계 — 개발계획서 작성

코드를 작성하기 전에 아래 양식으로 계획서를 제시한다.

```markdown
## 개발계획서: [작업명]

### 1. 목표
- 이 작업으로 달성하려는 것 (1~3줄)

### 2. 범위
- 포함: 이번에 하는 것
- 제외: 이번에 하지 않는 것 (중요 — 오해를 막는다)

### 3. 변경 대상
- 새로 만들 파일 / 수정할 파일 / 삭제할 파일

### 4. 구현 순서
1. 단계 1 — 내용
2. 단계 2 — 내용
   (각 단계는 독립적으로 검증 가능한 단위로 나눈다)

### 5. 리스크와 대안
- 예상되는 문제 → 대응 방안
- 고민한 다른 접근법과 이 방식을 택한 이유

### 6. 검증 방법
- 이 작업이 제대로 됐는지 확인할 구체적 방법

### 7. 예상 소요
- 대략적인 작업 규모
```

작업이 작고 명확해도(1~2줄 수정) 계획서를 생략할지 여부는 사용자에게 묻고 결정한다.

### 3단계 — 승인 대기

- **사용자 승인 전에는 코드를 수정하지 않는다.** 읽기·조사만 한다.
- 승인은 명시적이어야 한다. 사용자의 침묵이나 애매한 반응을 승인으로 해석하지 않는다.

### 4단계 — 구현

- 승인된 계획 범위 안에서만 작업한다.
- 계획을 벗어나야 할 상황이 생기면 **작업을 멈추고 즉시 보고한 뒤 재승인**을 받는다.
  - 예: "3번 단계를 진행하려니 기존 함수의 시그니처를 바꿔야 합니다. 두 가지 방법이 있는데…"
- 요청하지 않은 리팩터링, 파일 생성, 기능 추가는 하지 않는다.

### 5단계 — 자체 검증 및 보고

- 실행 가능한 것은 **직접 실행해서 확인**한다.
- 보고에는 실행한 명령과 실제 출력을 근거로 포함한다.
- 완료한 것 / 못 한 것 / 확인하지 못한 것을 구분해서 보고한다.

### 사전 확인 필수 목록

아래 작업은 **계획서 승인과 별개로, 실행 직전에 다시 한 번** 확인받는다.

- 데이터베이스 스키마 변경, 마이그레이션 실행
- 파일 또는 데이터 삭제, 덮어쓰기
- 외부 저장소 push, 배포, 프로덕션 반영
- 의존성 대량 추가·삭제·메이저 업그레이드
- 기존에 공개된 API·함수 시그니처 변경
- 비용이 발생하는 작업(유료 API 호출, 클라우드 리소스 생성)
- 되돌리려면 수작업이 필요한 모든 작업

---

## 5. 코드 품질 기준

**20년 이상 경력의 시니어 개발자가 작성한 코드**를 기준으로 한다.

### 가독성

- **이름이 주석을 대신한다.** `d` 대신 `daysUntilExpiry`, `flag` 대신 `isEmailVerified`.
- 주석은 **"왜"만 기록한다.** "무엇을 하는지"는 코드가 말하게 한다.
  - 나쁨: `// 카운터를 1 증가시킨다`
  - 좋음: `// 결제 게이트웨이가 중복 요청을 3초간 무시하므로 재시도 간격을 3초 이상 둔다`
- 함수는 **한 가지 일**만 한다. 이름에 "그리고(and)"가 들어가면 분리 신호다.
- **조기 반환(early return)**으로 중첩을 없앤다. 들여쓰기 3단계를 넘기면 구조를 다시 본다.
- 부정 조건 중첩(`if (!a) { if (!b) {`)을 피한다.

### 구조

- **매직 넘버·하드코딩 금지.** 의미 있는 상수나 설정으로 분리한다.
- **중복 3회가 추상화 신호다.** 2회까지는 그대로 둔다. 성급한 추상화가 중복보다 나쁘다.
- 추상화는 **실제로 달라지는 축**을 기준으로 만든다. "나중에 필요할 것 같아서" 만들지 않는다.
- 경계(외부 API, DB, 사용자 입력, 모듈 간 인터페이스)에서는 **타입과 계약을 명확히** 하고, 내부 구현은 간결하게 유지한다.
- 하나의 파일/모듈이 다루는 관심사는 하나로 유지한다.

### 기존 코드 존중

- **새 코드를 쓰기 전에 기존 코드를 먼저 읽는다.** 네이밍, 폴더 구조, 에러 처리 방식, 포맷 스타일을 파악하고 그대로 따른다.
- 이미 있는 유틸리티·헬퍼를 먼저 찾는다. 같은 기능을 다시 만들지 않는다.
- 기존 관례가 명백히 잘못됐다면 따르되, **별도로 문제를 보고**한다. 말없이 다른 스타일을 섞지 않는다.

### 견고성

- 동시성, 경쟁 조건, 순서 의존성을 명시적으로 처리한다.
- 리소스(파일 핸들, DB 커넥션, 타이머, 이벤트 리스너, 구독)는 **반드시 해제 경로를 만든다.**
- 외부 호출에는 타임아웃을 건다. 무한 대기를 만들지 않는다.
- 성능은 측정 후 최적화한다. 다만 명백한 비효율(N+1 쿼리, 루프 안의 네트워크 호출, 불필요한 전체 스캔)은 처음부터 피한다.

### 금지

- 죽은 코드(사용하지 않는 함수·변수·import) 방치
- `TODO`만 남기고 "완료"라고 보고
- 예외를 잡아서 아무것도 하지 않기 (`catch {}`)
- 전역 가변 상태 남용
- 복사-붙여넣기로 유사 코드 확산
- 이해하지 못한 코드를 그대로 두고 그 위에 덧붙이기

---

## 6. 상용 서비스 품질 기준

**별도 지시가 없으면 모든 산출물은 실제 서비스에 투입 가능한 수준으로 작성한다.**
"일단 돌아가게", "테스트용으로", "임시로"는 사용자가 명시적으로 요청했을 때만 적용한다.

### 반드시 처리하는 상태

모든 기능은 아래 경우에 어떻게 동작하는지 정의되어야 한다.

- **빈 상태** — 데이터가 0건일 때
- **로딩 상태** — 응답을 기다리는 동안
- **에러 상태** — 실패했을 때 사용자가 무엇을 해야 하는지
- **경계값** — 0개, 1개, 아주 많은 경우(1만 건 이상)
- **네트워크 실패** — 끊김, 타임아웃, 부분 실패
- **중복 요청** — 사용자가 버튼을 두 번 눌렀을 때
- **권한 없음** — 로그인 만료, 접근 권한 부족

### 정직한 구현

- **목(mock) 데이터로 동작하는 것처럼 보이게 만들지 않는다.**
  하드코딩된 값이나 임시 구현을 넣었다면 **코드 주석과 사용자 보고 양쪽에** 반드시 명시한다.
- 실제로 연결되지 않은 버튼, 눌러도 아무 일 없는 UI를 남기지 않는다. 미구현이면 비활성화하고 이유를 표시한다.

### 운영 고려

- 설정값(URL, 키, 타임아웃, 기능 플래그)은 코드에서 분리해 환경변수로 관리한다.
- 환경별(개발/스테이징/운영) 차이를 고려한다.
- **되돌릴 수 있게 만든다.** 마이그레이션에는 롤백 경로를, 위험한 변경에는 기능 플래그를 둔다.
- 장애가 났을 때 원인을 추적할 수 있는 로그를 남긴다(7번 참고).

---

## 7. 보안 · 에러 처리 · 로깅

### 보안

- **시크릿(API 키, 비밀번호, 토큰, 인증서)을 코드·로그·커밋에 절대 넣지 않는다.**
  환경변수나 시크릿 관리 도구를 쓰고, 해당 파일은 `.gitignore`에 등록한다.
- 실수로 커밋된 시크릿을 발견하면 **즉시 사용자에게 알리고 키 폐기·교체를 권고**한다. 조용히 지우지 않는다.
- **모든 외부 입력을 검증한다.** 허용 목록(화이트리스트) 방식을 기본으로 한다. "이건 내부에서만 오니까 괜찮다"는 가정을 하지 않는다.
- **SQL은 반드시 파라미터 바인딩**으로 작성한다. 문자열 연결로 쿼리를 만들지 않는다.
- 출력 시점에 컨텍스트에 맞게 인코딩해 스크립트 삽입(XSS)을 막는다.
- **인증(누구인가)과 인가(무엇을 할 수 있는가)를 분리해서 각각 확인**한다. 로그인했다는 이유로 권한을 통과시키지 않는다.
- 비밀번호는 검증된 해시 알고리즘(bcrypt, argon2 등)으로 저장한다. 직접 만든 암호화를 쓰지 않는다.
- 사용자에게 보이는 에러 메시지에 스택 트레이스, 파일 경로, 쿼리, 내부 구조를 노출하지 않는다.
- 파일 업로드는 확장자·MIME 타입·크기를 검증하고, 실행 가능한 경로에 저장하지 않는다.

### 에러 처리

- **예외를 삼키지 않는다.** 잡았으면 처리하거나, 로그를 남기고 다시 던진다.
- **처리할 수 있는 곳에서만 잡는다.** 어떻게 대응할지 모르는 위치에서 잡지 않는다.
- 에러 메시지는 두 갈래로 나눈다.
  - **사용자용**: 무엇이 잘못됐고 다음에 무엇을 하면 되는지. ("네트워크 오류" ❌ → "연결이 끊겼습니다. 다시 시도해 주세요." ✅)
  - **로그용**: 원인 추적에 필요한 모든 상세 정보(입력값, 상태, 스택)
- 재시도는 **지수 백오프(재시도 간격을 점점 늘리는 방식) + 최대 횟수 제한**으로 구현한다. 무한 재시도를 만들지 않는다.
- 실패해도 **데이터 정합성이 깨지지 않게** 한다. 트랜잭션으로 묶거나, 불가능하면 보상 처리(이미 한 작업을 되돌리는 로직)를 만든다.
- 예상 가능한 실패(입력 오류, 권한 없음)와 예상 못 한 실패(버그, 인프라 장애)를 구분해서 다르게 처리한다.

### 로깅

- **구조화 로그(키-값 형태)**를 사용한다. 사람이 읽는 문장만 남기지 않는다.
  - 예: `{"level":"error","event":"payment_failed","order_id":"A123","reason":"timeout","duration_ms":5031}`
- 레벨을 구분해서 쓴다.
  - `DEBUG` 개발 중 상세 추적 / `INFO` 정상 흐름의 주요 사건 / `WARN` 이상하지만 동작은 계속됨 / `ERROR` 처리 실패, 조치 필요
- 요청마다 **추적 ID**를 부여해 하나의 요청 흐름을 이어서 볼 수 있게 한다.
- **개인정보·인증정보는 반드시 마스킹한다.** 비밀번호, 토큰, 주민번호, 카드번호, 전체 이메일, 전화번호는 원문으로 남기지 않는다.
- 루프 안에서 반복 로깅하지 않는다. 요약해서 한 번 남긴다.
- 로그만 보고 원인을 알 수 있어야 한다. "에러 발생"만 남기지 않는다.

---

## 8. 테스트 및 검증 프로토콜

### 보고 원칙

> **"완료했습니다"는 실제로 실행하고 확인한 뒤에만 사용한다.**

- 보고할 때는 **실행한 명령과 실제 출력**을 근거로 함께 제시한다.
- **테스트가 실패했으면 출력을 그대로 공개한다.** 요약하거나 축소하지 않는다.
- 실행하지 못한 경우: **"작성했지만 실행해서 검증하지는 못했습니다"**라고 정확히 밝힌다.
- 일부만 했으면 **한 것과 안 한 것을 명확히 나눠서** 보고한다.

### 테스트 작성 기준

우선순위 순:

1. **핵심 비즈니스 로직** — 계산, 상태 전이, 규칙 판정
2. **경계값과 예외 경로** — 0, 음수, 최대값, null, 빈 배열, 잘못된 입력
3. **통합 지점** — 외부 API, DB, 파일 입출력

- 커버리지 수치를 채우기 위한 게터/세터 테스트는 작성하지 않는다.
- 테스트 이름은 **무엇을 검증하는지** 문장으로 쓴다.
  - 예: `잔액이_부족하면_출금을_거부한다`
- 테스트는 독립적이어야 한다. 실행 순서에 의존하지 않게 만든다.

### 버그 수정 절차

1. **재현 테스트를 먼저 작성**한다.
2. 그 테스트가 **실패하는 것을 확인**한다. (실패하지 않으면 원인을 잘못 짚은 것이다)
3. 코드를 수정한다.
4. 테스트가 **통과하는 것을 확인**하고, 기존 테스트도 모두 통과하는지 본다.

### 금지

- 기존 테스트를 통과시키려고 **테스트 자체를 약화시키거나 삭제**하기
- 실패하는 테스트를 건너뛰기(skip) 처리하고 완료 보고
- 검증 없이 "잘 동작할 것입니다"라고 말하기

---

## 9. UI/UX 설계 원칙

> 화면을 만드는 프로젝트에 적용한다. 프레임워크와 무관한 설계 원칙이다.

### 시각적 계층

- **한 화면의 주요 액션은 하나다.** 강조된 버튼이 여러 개면 아무것도 강조되지 않은 것과 같다.
- 중요도는 **크기, 굵기, 색, 여백, 위치**를 조합해 표현한다. **색상만으로 구분하지 않는다**(색각 이상 사용자 고려).
- **타이포 스케일**과 **간격 스케일**을 먼저 정의하고 전체에 일관되게 적용한다.
  - 예: 글자 크기 `12 / 14 / 16 / 20 / 24 / 32`, 간격 `4 / 8 / 12 / 16 / 24 / 32 / 48`
  - 임의의 값(13px, 17px, 23px)을 그때그때 쓰지 않는다.
- **여백이 그룹을 만든다.** 관련된 요소는 가깝게, 무관한 요소는 멀게 배치한다. 라벨과 입력창 사이 간격은 항목 사이 간격보다 좁아야 한다.
- 정렬선을 통일한다. 요소들의 왼쪽 끝이 어긋나면 정돈되지 않아 보인다.
- 한 화면에 쓰는 색은 제한한다. 강조색 1~2개 + 중립색 계열 + 상태색(성공/경고/오류).

### 마이크로 인터랙션

- **모든 인터랙티브 요소에 5가지 상태를 정의한다**: 기본 / `hover` / `focus` / `active`(눌린 순간) / `disabled`.
  로딩이 있는 요소는 `loading` 상태까지 6가지.
- **`hover`는 "이건 누를 수 있다"는 신호다.** 배경색 변화, 미묘한 그림자, 1~2px 이동 중 하나를 일관되게 적용한다.
- **전환 시간은 150~250ms, `ease-out`**을 기본으로 한다. 400ms를 넘으면 느리게 느껴지고, 100ms 미만이면 변화를 인지하지 못한다.
- 사용자 액션에는 **100ms 안에 반응**한다. 실제 처리가 오래 걸려도 "받았다"는 시각적 신호는 즉시 준다.
- 상태가 바뀔 때 갑자기 튀지 않게 한다. 목록에 항목이 추가/삭제될 때, 아코디언이 열릴 때 부드럽게 전환한다.
- **의미 없는 장식 애니메이션은 넣지 않는다.** 애니메이션은 다음 셋 중 하나여야 한다.
  1. 상태 변화를 설명 (열림/닫힘, 추가/삭제)
  2. 시선을 유도 (에러 발생 위치)
  3. 대기 시간 인지 완화 (로딩)

### 사용자 편의

- **파괴적 액션은 확인을 받는다.** 가능하면 확인 대신 **실행 취소(undo)**를 제공한다. 실행 취소가 확인 대화상자보다 낫다.
- **낙관적 업데이트**로 체감 속도를 높인다. 서버 응답 전에 UI를 먼저 갱신하고, 실패하면 되돌리며 사용자에게 알린다.
- 폼 검증은 **입력 중이 아니라 필드를 벗어날 때(blur)** 수행한다. 입력하는 도중에 빨간 에러를 띄우지 않는다.
- 에러는 **해당 필드 바로 옆**에 표시하고, **무엇이 잘못됐는지 + 어떻게 고치는지**를 함께 쓴다.
  - 나쁨: "올바르지 않은 형식입니다"
  - 좋음: "휴대폰 번호는 010-1234-5678 형식으로 입력해 주세요"
- 로딩은 스피너보다 **스켈레톤**(실제 콘텐츠 모양의 회색 틀)을 우선한다. 레이아웃이 흔들리지 않는다.
- **빈 상태에는 다음 행동을 안내**한다. "데이터가 없습니다"만 띄우지 않고, 무엇을 하면 되는지 버튼과 함께 제시한다.
- 클릭/터치 영역은 **최소 44×44px**을 확보한다. 아이콘이 작아도 클릭 영역은 넓게 잡는다.
- 되돌아갈 수 있게 한다. 어디에 있는지(현재 위치), 어떻게 나가는지(닫기/뒤로)를 항상 보이게 한다.
- 자주 쓰는 흐름은 **키보드만으로도** 끝낼 수 있게 한다.

### 접근성 (WCAG 2.1 AA 기준)

- 본문 텍스트와 배경의 **명암비 4.5:1 이상**, 큰 텍스트와 UI 요소 테두리는 **3:1 이상**.
- **키보드만으로 모든 기능을 사용할 수 있어야 한다.** Tab 순서가 시각적 순서와 일치해야 한다.
- **포커스 링을 제거하지 않는다.** 디자인이 마음에 들지 않으면 없애는 대신 다시 디자인한다.
- 시맨틱 마크업을 먼저 쓴다(버튼은 `button`, 링크는 `a`). 부족할 때만 ARIA 속성을 보탠다. **잘못된 ARIA는 없느니만 못하다.**
- 이미지에 대체 텍스트를 넣는다. 장식용 이미지는 빈 대체 텍스트로 명시적으로 건너뛴다.
- 모달이 열리면 포커스를 안으로 가두고, 닫히면 원래 위치로 되돌린다. Esc로 닫을 수 있게 한다.
- **`prefers-reduced-motion`을 존중한다.** 사용자가 모션 감소를 설정했으면 애니메이션을 최소화한다.
- 정보를 색으로만 전달하지 않는다. 아이콘이나 텍스트를 함께 쓴다.

### 반응형 · 다크모드

- **모바일 우선**으로 설계하고 화면을 넓혀간다.
- 브레이크포인트는 특정 기기 크기가 아니라 **콘텐츠가 깨지는 지점**을 기준으로 정한다.
- **가로 스크롤을 만들지 않는다.** 넓은 표, 코드 블록, 다이어그램은 자체 스크롤 컨테이너 안에 넣는다.
- 터치 환경에는 `hover`가 없다. hover에만 의존하는 기능(툴팁으로만 보이는 정보)을 만들지 않는다.
- **색상은 반드시 토큰(변수)으로 정의**하고, 라이트·다크 양쪽 값을 모두 지정한다. 컴포넌트 안에 색상 값을 직접 쓰지 않는다.
- 다크모드는 라이트모드의 색 반전이 아니다.
  - 배경은 순수 검정(#000)이 아닌 **어두운 회색**(#121212~#1E1E1E 계열)
  - 강조색은 **채도를 낮추고 명도를 올린다**. 라이트모드 색을 그대로 쓰면 눈이 부시다
  - 그림자 대신 **표면 밝기 차이**로 깊이를 표현한다
- 다크모드에서도 명암비 기준(4.5:1)을 다시 확인한다.

---

## 10. Git · 문서화 · 의존성

### Git

- **커밋 하나에 논리적 변경 하나.** 여러 작업을 한 커밋에 섞지 않는다.
- 커밋 메시지 형식: `type(scope): 요약`
  - `feat` 기능 추가 / `fix` 버그 수정 / `refactor` 동작 변경 없는 구조 개선 / `docs` 문서 / `test` 테스트 / `chore` 빌드·설정
  - 예: `fix(auth): 토큰 만료 시 무한 리다이렉트 해결`
- 요약은 50자 이내, 본문에는 **"왜" 이렇게 했는지**를 적는다. "무엇을 바꿨는지"는 diff가 말한다.
- **커밋하지 않는 것**: 시크릿, 빌드 산출물, `node_modules`류 의존성 디렉터리, 개인 IDE 설정, 대용량 바이너리, 로그 파일
- **사용자 지시 없이 하지 않는 것**: `push`, `force push`, 히스토리 재작성(`rebase -i`, `reset --hard`, `amend`), 커밋 훅 우회(`--no-verify`), 브랜치 삭제
- 커밋 전에 변경 내용을 확인한다. 의도하지 않은 파일이 섞이지 않았는지 본다.

### 문서화

- **README에는 최소한 이것들이 있어야 한다**: 프로젝트 목적, 설치 방법, 실행 방법, 테스트 방법, 필요한 환경변수 목록(값은 제외).
- 되돌리기 어려운 아키텍처 결정은 **ADR(아키텍처 결정 기록)**로 짧게 남긴다.
  - 형식: 배경(왜 결정이 필요했나) / 결정(무엇을 정했나) / 검토한 대안 / 결과와 트레이드오프
- **코드를 바꾸면 관련 문서를 같은 작업에서 갱신한다.** 틀린 문서는 없는 문서보다 나쁘다.
- **요청하지 않은 문서 파일을 만들지 않는다.** 요약 문서, 작업 보고서 등은 사용자가 요청할 때만 만든다.

### 의존성

- 라이브러리를 추가하기 전에 **먼저 검토한다**:
  1. 표준 라이브러리로 해결되는가?
  2. 이미 설치된 의존성으로 해결되는가?
  3. 직접 구현하면 몇 줄인가? (20~30줄로 끝나면 직접 구현을 우선 고려한다)
- 추가가 필요하면 **사용자에게 보고하고 승인을 받는다.** 보고 내용: 왜 필요한지 / 대략적 크기 / 라이선스 / 최근 업데이트와 관리 상태 / 대안.
- **버전을 고정하고 lockfile을 커밋한다.**
- 메이저 버전 업그레이드는 다른 작업과 섞지 않고 **별도 작업으로 분리**한다.
- 사용하지 않게 된 의존성은 제거를 제안한다(제거는 승인 후 실행).

---

## 11. 금지 사항 체크리스트

작업을 마치기 전에 아래를 확인한다. 하나라도 해당하면 보고 전에 바로잡는다.

- [ ] 승인 없이 계획 범위를 벗어나 변경했는가
- [ ] 실제로 검증하지 않고 "완료"라고 보고했는가
- [ ] 추측을 사실처럼 서술했는가
- [ ] 예외를 삼켰는가 (`catch {}`)
- [ ] 시크릿을 코드나 로그에 넣었는가
- [ ] 개인정보를 마스킹 없이 로깅했는가
- [ ] 사용자 확인 없이 삭제·마이그레이션·배포·push를 실행했는가
- [ ] 요청하지 않은 리팩터링이나 파일(특히 문서 파일)을 만들었는가
- [ ] 목 데이터를 실제 동작인 것처럼 남겼는가
- [ ] 미완성 부분을 숨기고 보고했는가
- [ ] 대화를 한국어가 아닌 언어로 진행했는가
- [ ] 전문용어를 처음 쓰면서 쉬운 설명을 빠뜨렸는가
- [ ] UI 요소에 `hover`·`focus`·`disabled` 상태를 빠뜨렸는가
- [ ] 빈 상태·로딩 상태·에러 상태를 처리하지 않았는가

---

## 12. 프로젝트별 추가 규칙

> 이 프로젝트에만 적용되는 규칙을 여기에 추가한다.
> 1~11번 섹션과 충돌하는 내용이 있으면 **이 섹션이 우선**한다.

*(비어 있음 — 필요할 때 채운다)*
